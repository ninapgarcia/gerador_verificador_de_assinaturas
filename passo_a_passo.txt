
1. rsa: gera p e q e a partir delas as chaves privada (e, n) e pública (d, n) 

2. gera a chave de sessao k
        - essa chave pode ter 128, 192 ou 256 bits e dependendo do tamanho da chave muda o numero de rounds do AES

3. cifra a mensagem usando AES

4. cifrar a chave k com o OEAP -> com a chave publica

5. fazer o hash (SHA3) da mensagem -> essa parte me confundiu

6. cifrar o hash da mensagem com o RSA -> ???

7. o que é mandado para decifrar é:
        - a chave k cifrada com OEAP
        - a mensagem cifrada com AES
        - o hash da mensagem cifrado com OEAP
        - a chave privada (que pelo que entendi a pessoa á teria por que a gnt usou a chave publica dela para cifrar)

8. usar a chave privada para decifrar a chave k (OEAP)

9. usar a chave privada para decifrar o hash da mensagem (OEAP)

10. usar a chave k decifrada para decifrar a mensagem cifrada

